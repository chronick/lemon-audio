{
  "name": "@lemon-audio/flasher",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && rm -rf ../website/public/flasher && cp -r dist ../website/public/flasher",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/w3c-web-usb": "^1.0.13",
    "typescript": "~5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.1.0"
  }
}
